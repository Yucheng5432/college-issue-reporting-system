<main>
    <h1>All Posts</h1>
    {{#if posts.length}}
    {{#each posts}}
    <div class="card">
        <h4>{{title}}</h4>
        <h5>{{username}}</h5>
        <p>{{body}}</p>  
    </div>
    {{/each}}
    {{/if}}

</main>