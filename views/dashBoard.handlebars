<div class="dashboard">
    <div class="header-nav">
      <ul class= "menu">
        <li>
          <a href="/myprofile" class="header">Welcome, {{username}}</a>
        </li>
        <li class="search">
            <form id="searchForm" class="searchForm" method="get">
                <label for="search">Search Post:</label>
                <input type="text" id= "search">
                <button type="submit">search</button>
            </form>
        </li>

        <li class="logout-li">
            <a  href="/logout">Logout</a>
        </li>
      </ul>
    </div>

   <div class="nav">
     <div class="navList">
       <p></p>
        <p>
            <a href="/dashboard" style="color: black;text-decoration: none;font-size: 20px;">Dashboard</a>
        </p>
        <p>
            <a href="/myprofile" style="color: black;text-decoration: none; font-size: 20px;">My Profile</a>
        </p>
      </div>
  </div>

 <div class="createPost">
    <h2>Create a Post</h2>
  <div class="createpostForm">  
      <form id="createpostForm">
        <input type="text" id="createpostTitle" name="createpostTitle" placeholder="Title" style="width: 400px; height: 30px;"><br>
        <textarea type="text" id="createpostBody" name="createpostBody" placeholder="Description" style="width: 400px; height: 60px;"></textarea><br>
        <textarea type="text" id="createpostTag" name="createpostTag" placeholder="Tag" style="width: 400px; height: 60px;"></textarea><br>
        <button type="submit">Post</button>
      </form>
  </div>
    {{#if error}}
      <div id="createPost-error" class="error">
          <h3>{{error_message}}</h3>
      </div>
    {{/if}}
  </div>

  <div class="private">
   <h2>All Posts</h2>

          {{#if posts.length}}
          {{#each posts}}
              <div class="card">
                  <div class="innercard">
                      <div class="poststitle-date-div">
                            <label class="postsTitle">{{title}}</label><br>
                            <label class="postsdate">by {{username}} on {{date}}</label>
                      </div>
                            <p>{{body}}</p> 
                            <label>Tag:</label>
                            {{#if tags.length}}
                            {{#each tags}}
                            <label class="taglable">{{this}}</label>
                            {{/each}}
                            {{/if}}
                      <div class="commentsCard">
                            <br>
                            <label class="comments">Comments</label>
                            <br>
                        {{#if comments.length}}
                        {{#each comments}}
                            <label>{{userName}}:</label>
                            <textarea>{{body}}</textarea>
                        {{/each}}
                        {{/if}}
                        <label class="reply">Reply:</label>
                        <textarea class="createComment"></textarea>
                        <button type="submit">Submit</button>
                      </div>
                  </div>
              </div>
          {{/each}}
          {{/if}}

  </div>

</div>

